def sorteio(lista):
    c = 0
    from random import randint
    for c in range(c, d):
        n.append(randint(0, 10))
    print(f'Foram sorteados {d} números formando a lista {n}.')


def somapar(lista):
    par = 0
    for v in n:
        if v % 2 == 0:
            par += v
    print(f'A soma dos números pares é {par}.')


n = list()
d = int(input('Digite um número: '))
sorteio(n)
somapar(n)
